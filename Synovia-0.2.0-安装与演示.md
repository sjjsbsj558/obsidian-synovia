# Synovia 0.2.0 安装与演示

## 一、导入插件

1. 完全退出 Obsidian，再重新打开目标库。
2. 打开 `设置 → 社区插件`，确认已关闭安全模式。
3. 关闭 Synovia（如果列表里仍显示旧条目），然后退出并重新打开 Obsidian。
4. 在目标库中打开插件目录：
   - `设置 → 社区插件 → 已安装插件 → 文件夹图标`
   - 或直接进入目标库的 `.obsidian/plugins/`
5. 将压缩包中的整个 `synovia` 文件夹复制到：

   `.obsidian/plugins/synovia/`

   最终目录至少应包含：

   ```text
   .obsidian/plugins/synovia/
   ├── main.js
   ├── manifest.json
   ├── styles.css
   └── skills/obsidian-markdown/
   ```

6. 回到 Obsidian 的社区插件列表，刷新插件列表并启用 `Synovia`。
7. 如果没有立即出现，执行命令面板中的 `重新加载当前库`，或完全重启 Obsidian。

压缩包已经包含顶层 `synovia/` 文件夹，不要把 ZIP 文件本身复制到插件目录，也不要多套一层目录。

## 二、首次配置

打开 `设置 → 社区插件 → Synovia`。

### 模型

填写以下三项：

- `允许远程调用`：打开。
- `兼容 Chat Completions 的服务根地址`：例如 `https://你的服务/v1`。
- `模型名称`：填写服务商实际支持的模型 ID。
- `模型密钥`：点击密钥输入控件，保存一个 Obsidian Secret，并选择它。

地址可以填写服务根地址，也可以填写完整的 `/chat/completions` 地址。不要把 API Key 直接写进地址或演示笔记。

建议先使用以下范围，避免演示时输入过长：

- `候选笔记数量`：5 到 10。
- `知乎搜索条数`：5。
- `请求超时（秒）`：90。
- `每次模型请求的输入字符上限`：按服务能力设置；首次演示可使用默认值。
- `每次模型请求的输出 Token 上限`：按服务能力设置；首次演示可使用默认值。

填写完点击页面底部的 `保存设置`。

### 知乎

默认连接方式是 `知乎 CLI / Skill`：

- `知乎 CLI 命令` 保持 `zhihu-cli`，Windows 会尝试自动发现官方安装位置。
- 如果自动发现失败，先在官方知乎 CLI 环境中完成登录，再把 `status` 返回的 `binary_path` 绝对路径填入此处。
- 如果 CLI 已经登录，`知乎密钥（CLI 已登录时可留空）` 可以留空。
- 也可以改用 `HTTP 搜索 API` 或 `MCP SSE`，但要按对应服务填写密钥和连接环境。

演示前先确认模型服务和知乎连接都能访问。知乎返回的是搜索摘要和来源链接，不是完整文章；因此演示时应把知乎内容作为一种外部观点来源，与本地笔记一起比较，而不是当作唯一答案。

## 三、3 到 5 分钟演示流程

### 1. 打开入口

在命令面板运行 `Synovia: 打开工作台`，或点击左侧 Ribbon 上的 Synovia 图标。

主界面只有一个核心动作：输入问题，点击 `形成观点`。

### 2. 使用两篇本地大笔记

确保两篇笔记已经在当前库中，并且内容分别包含不同倾向、不同条件或不同经验。

在 `你想判断什么？` 中输入：

```text
远程工作是否适合需要持续专注、又有高频协作的团队？请比较本地笔记和知乎中的不同观点，给出明确判断，并说明适用边界、主要分歧和仍需验证的问题。
```

点击 `形成观点`。

Synovia 会：

1. 从本地 Markdown 笔记中找相关材料。
2. 在允许远程调用时自动联动知乎，即使本地已经找到相关笔记。
3. 将本地材料和知乎材料放在同一组证据中交给 Agent。
4. 让 Agent 先比较支持、反对、条件和冲突，再形成一个有边界的判断。
5. 在保存前核对引用是否确实出现在来源原文中。

这里的核心演示点是：**本地和知乎只是知识来源，Agent 不按来源类型预先选边，而是让不同观点在同一个问题下碰撞。**

### 3. 展示结果

结果区域重点看四处：

- 观点标题和 `stance`：Agent 最终采取的判断方向。
- `依据`：展开后可查看本地或知乎来源、链接和原文摘录。
- `分歧与边界`：展示不同观点在哪些条件下成立。
- `待验证`：展示证据不足、需要继续检查或不能直接推广的部分。

演示时可以点击来源链接，说明每个关键判断都应能回到原文，而不是只看模型的流畅表述。

### 4. 保存观点

点击 `保存到 Wiki`，再点击 `打开已保存的观点`。

默认保存位置是：

```text
Wiki/Opinions/
```

原始笔记不会被覆盖。保存的是一份新的观点笔记，便于后续复盘、修改和继续验证。

### 5. 演示知乎补充本地空白

再次输入一个本地笔记没有直接讨论的问题，例如：

```text
对于已经实行远程工作的团队，哪些管理动作最容易造成隐性协作成本？
```

如果本地没有直接命中，Synovia 会尝试补充知乎来源；结果中应明确显示知乎来源或失败状态。

如果知乎连接失败但本地已有证据，插件可能继续使用本地材料，但界面会保留知乎失败提示。演示时不要把这种结果说成“已经完成知乎联动”。

## 四、常用操作

- `打开 Synovia`：打开侧边栏。
- `Synovia: 打开工作台`：从命令面板打开侧边栏。
- `用选区形成观点`：在笔记中选中一段文字后，从命令面板直接把选区作为判断材料。
- `清除检索缓存`：需要重新检索时使用。
- 运行中的任务显示方形停止按钮；长时间等待时可停止。

## 五、演示注意事项

- 不要把 API Key 写进 Markdown、截图或投屏窗口。
- 不要展示 `.synovia` 目录；其中可能有私密输入和任务记录。
- 知乎结果是摘要和来源，不等于完整文章，也不保证代表知乎全部意见。
- Agent 的观点是基于当前提供材料的综合，不是事实核验或全社区共识。
- 如果模型没有配置好，插件不能形成完整观点；不要只根据关键词候选结果宣称已经完成语义分析。
